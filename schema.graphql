input RuleInput {
    rule: String!
    value: Int!
}

type VerifyResult {
    verify: Boolean!
    noMatch: [String]!
}

type Query {
    verify(password: String!, rules: [RuleInput!]): VerifyResult!
}

schema {
    query: Query
}